---
name: fact-check-cy
version: 1.0.0
description: |
  调研报告事实核查与质控 skill，完全基于市场调研 SOP P4 方法论
  （SIFT + CRAAP + 四维三角验证 + AI 幻觉 7 项检查）。
  自动核查报告中所有 URL 是否真实可达、数字是否能溯源、关键论点是否
  有≥2 个独立信源支撑、信源是否权威、有无自相矛盾。
  当用户说"事实核查"、"查证"、"核实数据"、"验证报告"、"溯源"、
  "看看 URL 有没有问题"、"这些数字靠不靠谱"、"三角验证"、"CRAAP"、
  "fact check"、"verify sources"、"核对引用"时触发。
  当用户刚完成 deep-research / market-researcher-cy / competitive-analyst-cy
  调研、即将交付报告或做关键决策时，主动建议使用此技能。
allowed-tools:
  - Read
  - Grep
  - Glob
  - Bash
  - WebFetch
  - WebSearch
---

# 调研报告事实核查 skill

调研产物交付前的最后一道防线。AI 生成的 URL 错误率约 60%，精确数字常有"创造性合并"幻觉——本 skill 自动执行 SOP P4 的四个核心方法（SIFT / CRAAP / 四维三角验证 / AI 幻觉 7 项 checklist），输出风险清单与行动建议，由人工拍板修改报告。

## 核心边界

- **做**：核查 URL 真实性、数字溯源、信源评估、三角验证、矛盾检测、输出风险清单
- **不做**：自动修复报告（只输出建议，人工决定怎么改）；重新调研（那是 deep-research 的事）

## 配置参数

用户可通过自然语言指定，也可使用默认值：

| 参数 | 取值 | 默认 | 说明 |
|------|------|------|------|
| `mode` | light / deep | light | 轻档 5 分钟 / 深档 15-20 分钟（增加 CRAAP 全 5 维 + 四维三角验证） |
| `target` | full / point | full | 整份报告核查 / 单点数据核查 |
| `whitelist_boost` | on / off | on | 启用必查信源白名单加权（艾瑞、QuestMobile、巨量算数等） |
| `language` | cn / en / both | both | 信源语言偏好 |

示例触发：
- "核查这份调研报告"（full + light，默认）
- "深度核查这份报告"（full + deep）
- "这个 940 万的数据是真的吗"（point）

## 执行流程（5 步）

### Step 1：解析输入

判断用户输入类型：

1. **整份报告核查**（`target: full`）：
   - 用户提供 markdown 文件路径 → 用 `Read` 读取
   - 用户粘贴报告内容 → 直接处理
   - 用户说"刚才那份报告" → 从对话历史中提取最近的报告文本

2. **单点核查**（`target: point`）：
   - 提取陈述中的"数据 + 来源"组合
   - 例如："中国程序员 940 万人，来自工信部" → 数据=940 万 / 主体=中国程序员 / 来源=工信部

输出结构化的核查任务清单（URL 列表、数字列表、核心信源列表），供后续步骤并行处理。

### Step 2：URL 健康度核查

> 这是最高优先级的步骤——AI 幻觉 URL 占比约 60%，不验证几乎一定有问题。

**做法**：

1. 用 `Grep` 或 `Bash` 正则从报告中**提取所有 URL**：
   - markdown 链接：`\[.*?\]\((https?://[^)]+)\)`
   - 纯文本 URL：`https?://[^\s\)\]"'，。、；]+`
   - 去重后得到 URL 清单

2. **并行**对每个 URL 跑 `WebFetch`：
   - 验证 HTTP 状态码（200 = 通过，404/403/5xx = 失败）
   - 拿到页面标题和正文摘要
   - 跟引用上下文（前后 50 字）做关键词重合度对比

3. 输出三类清单：
   - ✅ **可访问且相关**：状态 200，内容跟上下文重合度 ≥3 个关键词
   - ⚠️ **可访问但内容不匹配**：状态 200，但内容跟上下文不相关（最隐蔽的 AI 幻觉）
   - ❌ **失效**：404 / 403 / 5xx / DNS 解析失败

**关键约束**：URL 数量较多时（>20 个），分批并行（每批 5 个），避免一次发太多 WebFetch 请求。

### Step 3：精确数字溯源

> 防止 AI"创造性合并"出不存在的数字。

**做法**：

1. 用 `Grep` 提取报告中所有"精确数字"模式：
   - 金额：`\$\d+(\.\d+)?[BMK]?` / `¥\d+(\.\d+)?[万亿]?`
   - 百分比：`\d+(\.\d+)?%`
   - 大数字：`\d+(,\d{3})+` 或 `\d+\s*(万|亿|百万|千万|million|billion)`
   - 增长率：`(YoY|同比|环比|CAGR)\s*[+-]?\d+(\.\d+)?%`

2. 对每个数字找**最近的引用源**（前后段落里的 URL 或机构名）。

3. 用 `WebSearch` 反查：以"数字 + 主体 + 时间"作为查询，看是否能找到原始来源。
   - 例如：`"940 万" 中国程序员 工信部 2024`

4. 标记每个数字：
   - ✅ **已找到原文出处**：原始来源中能看到这个精确数字
   - ⚠️ **仅模糊匹配**：能找到相关数据，但不是精确同一个数字（差距 < 10%）
   - ❌ **原文未找到**：无法在引用源中找到，强提示 AI 幻觉风险

### Step 4：CRAAP 信源评估（仅深档执行完整 5 维）

> 对每个**核心信源**（被引用 ≥2 次的来源）做 5 维度评估。

**自动评分逻辑**（详见 [reference.md](./reference.md) §1）：

| 维度 | 评分依据 |
|---|---|
| **C**urrency 时效性 | WebFetch 拿发布日期 → 距今 <6 月 = 5 / 6-12 月 = 4 / 1-2 年 = 3 / 2-3 年 = 2 / >3 年 = 1 |
| **R**elevance 相关性 | 信源内容跟引用论点的关键词重合度（≥5 个 = 5 / 4 个 = 4 / 3 个 = 3 / 2 个 = 2 / 1 个 = 1） |
| **A**uthority 权威性 | 域名查询白名单（艾瑞 / QuestMobile / 巨量算数 / 36氪 / 上市公司财报 → 5 / 主流媒体 → 3-4 / 不知名博客 → 1-2） |
| **A**ccuracy 准确性 | 该论点是否有 ≥2 个独立信源支持（≥3 源 = 5 / 2 源 = 4 / 1 源 = 2） |
| **P**urpose 目的性 | 是否商业/营销/PR（学术/官方报告 = 5 / 中立媒体 = 4 / 行业分析 = 3 / 公司 PR = 2 / 利益方声明 = 1） |

每个核心信源得 0-25 总分。**红旗规则**：任意维度 ≤2 → 标红（reference.md §1 详述）。

**轻档模式跳过此步**，仅做最基础的 ≥2 源交叉检查。

### Step 5：三角验证 + 矛盾检测 + AI 幻觉 7 项 checklist + 输出报告

#### 5a. 三角验证（关键论点）

1. 自动识别报告中的"结论性论点"：
   - 加粗段落（`\*\*...\*\*`）
   - 含"研究表明"、"数据显示"、"调研发现"等关键词
   - 数字密度高的段落
2. 对每个论点统计支持它的**独立信源数**（按域名去重）
3. 标记：
   - ✅ ≥2 个独立信源 → 已三角验证
   - ⚠️ 1 个信源 → 强提示补反向源
   - ❌ 0 个信源 → 纯 AI 生成，必须删除或补源

#### 5b. 深档加做：四维三角验证

详见 [reference.md](./reference.md) §2，含 4 类（数据源 / 方法论 / 调查者 / 理论）的执行细节。

#### 5c. AI 幻觉 7 项 checklist

逐项 ✅ / ⚠️ / ❌ 标记，详见 [reference.md](./reference.md) §3：

1. **精确数字溯源**——每个数字能否追溯到原始来源
2. **张冠李戴检查**——数据是否被错误归属到其他公司/时期/市场
3. **来源链接验证**——已在 Step 2 完成
4. **逻辑一致性**——不同部分的数据/结论是否自洽
5. **过度外推检查**——是否将有限数据过度推广
6. **时间线正确性**——事件顺序、因果时序是否合理
7. **综合幻觉检查**——AI 是否把多个来源的数据"创造性合并"出不存在的结论

#### 5d. 矛盾检测

同一指标在报告不同段落多次出现时，对比数值。差异 >5% → 标记矛盾点。

#### 5e. 输出核查报告

按下方格式生成 markdown 报告，直接输出（不存档，除非用户明确要求）。

## 输出报告格式

```markdown
# 📋 事实核查报告

> **模式**：{light | deep}    **覆盖**：X 个 URL / Y 个数据点 / Z 个核心信源
> **核查时间**：YYYY-MM-DD HH:MM

## 总览

- ✅ 通过：X 项
- ⚠️ 待补强：X 项
- ❌ 不通过：X 项
- **综合可信度评分：X / 100**

## 一、URL 健康度（X / Y 个）

| URL（截断） | 状态 | 上下文匹配 | 风险 |
|---|---|---|---|
| https://... | ✅ 200 | ✅ 相关 | — |
| https://... | ❌ 404 | — | **必须删除引用** |
| https://... | ✅ 200 | ⚠️ 不匹配 | **疑似 AI 幻觉，必须核实** |

## 二、数据点溯源（X / Y 个）

| 数字 | 引用源 | 反查结果 | 风险等级 |
|---|---|---|---|
| 940 万程序员 | 工信部 2024 | ✅ 知乎/EET-China 多源确认 | 低 |
| $4.2B 市场规模 | 报告未注明 | ❌ 反查无结果 | **高（疑似幻觉）** |

## 三、CRAAP 评估（核心信源，仅 deep 模式）

| 信源 | C | R | A | A | P | 总分 | 红旗 |
|---|---|---|---|---|---|---|---|
| 艾瑞咨询 2025 报告 | 5 | 5 | 5 | 4 | 4 | 23 | — |
| 某不知名博客 | 3 | 4 | 1 | 1 | 2 | 11 | 🚩 权威性 / 准确性 |

## 四、三角验证缺口

**以下 X 个论点仅有 1 个信源支撑，建议补反向验证：**

1. "中国 IP 衍生品市场年增 18%" — 仅艾瑞一家来源
   - 建议补查：QuestMobile / 36氪 / 行业上市公司财报

## 五、AI 幻觉 7 项 checklist

- [✅] 精确数字溯源：8/10 个数字已确认来源
- [⚠️] 张冠李戴检查：发现 1 处时期错位
- [✅] 来源链接验证：见 §一
- [✅] 逻辑一致性
- [⚠️] 过度外推检查：1 处用 1 城样本推广全国
- [✅] 时间线正确性
- [❌] 综合幻觉检查：发现 1 处"创造性合并"

## 六、矛盾检测

| 指标 | 段落 A | 段落 B | 差异 |
|---|---|---|---|
| 程序员人数 | 940 万 | 1000 万 | 6.4%（>5% 阈值） |

## 七、行动建议（top 3 风险）

1. **🔴 必须删除**：URL https://... 返回 404，引用此 URL 的论点全部要核实
2. **🟠 必须补源**："$4.2B 市场规模"无原始出处，疑似 AI 编造
3. **🟡 建议核实**：940 万 vs 1000 万 程序员人数前后矛盾，统一口径

---

> 本报告由 fact-check-cy 自动生成。最终决策由人工拍板。
> 详细方法论参见 [SOP P4 验证质控](~/Documents/think/市场调研SOP-方法论与流程.md)
```

## 工具使用约束

| 工具 | 用途 | 注意事项 |
|------|------|----------|
| Read | 读 markdown 报告文件 | 仅读已知路径 |
| Grep | 提取 URL、数字、关键论点 | 用正则匹配 |
| Bash | 批量并行处理、轻量数据清洗 | 避免无谓的复杂脚本 |
| WebFetch | URL 健康度核查、信源内容拉取 | **每个 URL 必须验证**，分批并行（每批 5 个） |
| WebSearch | 数字反查、CRAAP 信源背景 | 中英双语；优先白名单信源 |
| Glob | 在用户工作目录定位报告文件 | 仅当用户说"刚才那份报告"时使用 |

## 全局约束

- **人工驱动**：不主动跑——除非用户触发或在主动建议时拒绝过的不主动二次推荐
- **明确标注无法验证**：所有 ⚠️ / ❌ 项必须给出"建议补查的信源"
- **不修复报告**：只输出风险清单和建议，不擅自改动原文
- **轻档优先**：默认走 light 模式，用户明确说"深度核查"才进 deep
- **白名单加权**：默认 `whitelist_boost: on`，详见 reference.md §4

## 详细参考

CRAAP 5 维评分细则、四维三角验证完整示例、AI 幻觉 7 项追问模板、名创业务必查信源白名单、输出报告完整模板：

见 [reference.md](./reference.md)
